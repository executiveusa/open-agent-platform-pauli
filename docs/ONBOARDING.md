# Onboarding

Onboarding is voice-first with text fallback. Sessions are stored server-side and must reach minimum required fields to unlock the dashboard.

Stages are defined in JSON templates under `/config/onboarding`.

# Operations

## Kill switches
- DISABLE_AGENT_RUNS
- DISABLE_AUTOMERGE
- FORCE_AUTONOMY_MODE

## Rollback
Use `/api/org/config/rollback` to restore a previous snapshot.

## Incident response
Disable runs and automerge using the kill switches, then rollback configs if needed.

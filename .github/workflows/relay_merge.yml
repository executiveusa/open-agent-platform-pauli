name: Relay Merge

on:
  pull_request:
    types: [labeled]

jobs:
  merge:
    if: contains(github.event.pull_request.labels.*.name, relay:safe)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-merge placeholder with policy gate" 
